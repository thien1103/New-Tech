import { Button, Form, Input, Table } from "antd";

const CreateCouncil = (props) => {
const { bg } = props;
return (
    <div
      className="flex flex-col"
      style={{
        padding: 24,
        minHeight: 360,
        background: bg,
      }}
        >
      <div className=" mb-2 pb-2 border-b-[1px] border-black border-solid"></div></div>
);
};

export default CreateCouncil